# Fetch certs + provisioning profiles from the shared signing repo
git_url("https://github.com/mieweb/mobile-signing")
storage_mode("git")
type("appstore")
git_basic_authorization(ENV["MATCH_GIT_BASIC_AUTHORIZATION"])
