# Environment Configuration for MieWeb Auth App

# Firebase Configuration
# Required: Firebase service account credentials in JSON format
# FIREBASE_SERVICE_ACCOUNT_JSON='{"type":"service_account","project_id":"your-project-id",...}'

# Root URL for the application (used for callbacks and links)
# ROOT_URL=https://your-domain.com

# MongoDB URL (if not using default Meteor MongoDB)
# MONGO_URL=mongodb://localhost:27017/mieweb_auth_app

# Email Configuration (for sending notifications)
# MAIL_URL=smtp://username:password@smtp.example.com:587

# Send Notification Authentication
# Set to 'true' to require API key authentication for the /send-notification endpoint
# When enabled, clients must provide 'apikey' and 'client_id' in POST requests
# Default: false (authentication optional)
SEND_NOTIFICATION_FORCE_AUTH=false

# Example production configuration:
# SEND_NOTIFICATION_FORCE_AUTH=true
